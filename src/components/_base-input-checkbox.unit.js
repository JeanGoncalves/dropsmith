import BaseInputCheckbox from './_base-input-checkbox'

describe('@components/_base-input-checkbox', () => {
  it('exports a valid component', () => {
    expect(BaseInputCheckbox).toBeAComponent()
  })
})
